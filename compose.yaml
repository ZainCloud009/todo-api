services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: todo-api:latest
    container_name: todo-api-service
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PORT=8000
